/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./*.html", // Tells Tailwind to scan all HTML files in the current directory
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
